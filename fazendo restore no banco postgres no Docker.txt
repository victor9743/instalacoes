fazendo restore no banco postgres no Docker

Se o arquivo estiver fora do container e quiser copiar primeiro
docker cp arquivo.sql postgres_db:/tmp/

Depois:
docker exec -it postgres_db bash

E dentro do container:
psql -U user -d banco_2024 < /tmp/arquivo.sql
